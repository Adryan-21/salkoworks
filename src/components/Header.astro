---
---

<!-- Skip Navigation Link for Accessibility -->
<a 
  href="#main-content" 
  class="sr-only focus:not-sr-only focus:absolute focus:top-4 focus:left-4 z-[100] bg-amber-600 text-white px-4 py-2 rounded-lg font-medium transition-all duration-200 focus:ring-4 focus:ring-amber-300"
>
  Przejdź do głównej treści
</a>

<header class="sticky top-0 z-50 bg-amber-50/80 backdrop-blur-xl border-b border-amber-200/30 shadow-lg shadow-amber-100/20 transition-all duration-300">
  <div class="container mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex items-center justify-between h-16 lg:h-20">
      <!-- Logo with Enhanced Interactions -->
      <a 
        href="#" 
        class="group flex items-center space-x-3 text-xl lg:text-2xl font-bold tracking-tight text-amber-900 hover:text-amber-700 transition-all duration-300 focus:outline-none focus:ring-4 focus:ring-amber-300/50 rounded-lg p-1"
        aria-label="SalkoWorks - Strona główna"
      >
        <div class="relative w-10 h-10 lg:w-12 lg:h-12 bg-gradient-to-br from-amber-600 via-amber-500 to-orange-500 rounded-xl flex items-center justify-center shadow-lg hover:shadow-xl transition-all duration-300 group-hover:scale-105 group-hover:rotate-3">
          <!-- Shine effect -->
          <div class="absolute inset-0 bg-gradient-to-br from-white/20 to-transparent rounded-xl opacity-0 group-hover:opacity-100 transition-opacity duration-300"></div>
          <span class="relative text-white font-bold text-sm lg:text-base drop-shadow-sm">SW</span>
        </div>
        <span class="font-extrabold bg-gradient-to-r from-amber-900 to-amber-700 bg-clip-text text-transparent">
          SalkoWorks
        </span>
      </a>

      <!-- Enhanced Desktop Navigation -->
      <nav class="hidden md:flex items-center space-x-2 lg:space-x-4" role="navigation" aria-label="Główna nawigacja">
        <a 
          href="#home" 
          class="nav-link group relative text-amber-800 hover:text-amber-600 px-4 py-2.5 rounded-xl text-sm lg:text-base font-medium transition-all duration-300 hover:bg-amber-100/60 focus:outline-none focus:ring-2 focus:ring-amber-300/50 focus:bg-amber-100/60"
          aria-describedby="home-desc"
        >
          <span class="relative z-10">Strona główna</span>
          <!-- Hover background animation -->
          <div class="absolute inset-0 bg-gradient-to-r from-amber-100/40 to-orange-100/40 rounded-xl opacity-0 group-hover:opacity-100 transition-all duration-300 scale-95 group-hover:scale-100"></div>
        </a>
        <a 
          href="#uslugi" 
          class="nav-link group relative text-amber-800 hover:text-amber-600 px-4 py-2.5 rounded-xl text-sm lg:text-base font-medium transition-all duration-300 hover:bg-amber-100/60 focus:outline-none focus:ring-2 focus:ring-amber-300/50 focus:bg-amber-100/60"
          aria-describedby="uslugi-desc"
        >
          <span class="relative z-10">Usługi</span>
          <!-- Hover background animation -->
          <div class="absolute inset-0 bg-gradient-to-r from-amber-100/40 to-orange-100/40 rounded-xl opacity-0 group-hover:opacity-100 transition-all duration-300 scale-95 group-hover:scale-100"></div>
        </a>
        <a 
          href="#o-mnie" 
          class="nav-link group relative text-amber-800 hover:text-amber-600 px-4 py-2.5 rounded-xl text-sm lg:text-base font-medium transition-all duration-300 hover:bg-amber-100/60 focus:outline-none focus:ring-2 focus:ring-amber-300/50 focus:bg-amber-100/60"
          aria-describedby="o-mnie-desc"
        >
          <span class="relative z-10">O mnie</span>
          <div class="absolute inset-0 bg-gradient-to-r from-amber-100/40 to-orange-100/40 rounded-xl opacity-0 group-hover:opacity-100 transition-all duration-300 scale-95 group-hover:scale-100"></div>
        </a>
        <a 
          href="#kontakt" 
          class="nav-link group relative text-amber-800 hover:text-amber-600 px-4 py-2.5 rounded-xl text-sm lg:text-base font-medium transition-all duration-300 hover:bg-amber-100/60 focus:outline-none focus:ring-2 focus:ring-amber-300/50 focus:bg-amber-100/60"
          aria-describedby="kontakt-desc"
        >
          <span class="relative z-10">Kontakt</span>
          <div class="absolute inset-0 bg-gradient-to-r from-amber-100/40 to-orange-100/40 rounded-xl opacity-0 group-hover:opacity-100 transition-all duration-300 scale-95 group-hover:scale-100"></div>
        </a>
        
        <!-- Enhanced CTA Button -->
        <a 
          href="#kontakt" 
          class="group relative overflow-hidden bg-gradient-to-r from-amber-600 to-orange-500 text-white px-6 lg:px-8 py-2.5 lg:py-3 rounded-full text-sm lg:text-base font-semibold transition-all duration-300 transform hover:scale-105 shadow-lg hover:shadow-xl hover:from-amber-700 hover:to-orange-600 focus:outline-none focus:ring-4 focus:ring-amber-300/50 ml-4"
          aria-label="Skontaktuj się, aby uzyskać bezpłatną wycenę"
        >
          <!-- Button shine effect -->
          <div class="absolute inset-0 bg-gradient-to-r from-transparent via-white/20 to-transparent -skew-x-12 translate-x-[-100%] group-hover:translate-x-[200%] transition-transform duration-700"></div>
          <span class="relative flex items-center space-x-2">
            <span>Bezpłatna wycena</span>
            <svg class="w-4 h-4 transition-transform duration-300 group-hover:translate-x-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
            </svg>
          </span>
        </a>
      </nav>

      <!-- Enhanced Mobile Menu Button -->
      <button 
        id="mobile-menu-button"
        class="md:hidden group relative w-12 h-12 rounded-xl bg-amber-100/80 hover:bg-amber-200/80 transition-all duration-300 flex items-center justify-center focus:outline-none focus:ring-4 focus:ring-amber-300/50 hover:scale-105 active:scale-95"
        aria-label="Otwórz menu nawigacyjne"
        aria-expanded="false"
        aria-controls="mobile-menu"
      >
        <!-- Enhanced hamburger with better accessibility -->
        <div class="hamburger">
          <span class="hamburger-line top-line" aria-hidden="true"></span>
          <span class="hamburger-line middle-line" aria-hidden="true"></span>
          <span class="hamburger-line bottom-line" aria-hidden="true"></span>
        </div>
        
        <!-- Loading indicator for better UX -->
        <div class="absolute inset-0 rounded-xl bg-amber-200/60 opacity-0 group-active:opacity-100 transition-opacity duration-150"></div>
      </button>
    </div>

    <!-- Enhanced Mobile Navigation -->
    <div 
      id="mobile-menu" 
      class="md:hidden absolute top-full left-0 right-0 bg-white/95 backdrop-blur-xl border border-amber-200/50 shadow-2xl shadow-amber-100/40 transform -translate-y-full opacity-0 invisible transition-all duration-500 ease-out overflow-hidden"
      role="navigation"
      aria-label="Menu mobilne"
    >
      <!-- Glass morphism overlay -->
      <div class="absolute inset-0 bg-gradient-to-b from-amber-50/80 to-cream-50/80 backdrop-blur-sm"></div>
      
      <div class="relative px-6 py-8 space-y-2">
        <!-- Mobile navigation links with enhanced styling -->
        <a 
          href="#home" 
          class="mobile-nav-link group flex items-center justify-between px-4 py-4 text-amber-800 hover:text-amber-600 hover:bg-amber-50/80 rounded-xl transition-all duration-300 focus:outline-none focus:ring-2 focus:ring-amber-300/50 focus:bg-amber-50/80"
          role="menuitem"
        >
          <span class="font-medium">Strona główna</span>
          <svg class="w-5 h-5 text-amber-400 transition-transform duration-300 group-hover:translate-x-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
          </svg>
        </a>
        <a 
          href="#uslugi" 
          class="mobile-nav-link group flex items-center justify-between px-4 py-4 text-amber-800 hover:text-amber-600 hover:bg-amber-50/80 rounded-xl transition-all duration-300 focus:outline-none focus:ring-2 focus:ring-amber-300/50 focus:bg-amber-50/80"
          role="menuitem"
        >
          <span class="font-medium">Usługi</span>
          <svg class="w-5 h-5 text-amber-400 transition-transform duration-300 group-hover:translate-x-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
          </svg>
        </a>
        <a 
          href="#o-mnie" 
          class="mobile-nav-link group flex items-center justify-between px-4 py-4 text-amber-800 hover:text-amber-600 hover:bg-amber-50/80 rounded-xl transition-all duration-300 focus:outline-none focus:ring-2 focus:ring-amber-300/50 focus:bg-amber-50/80"
          role="menuitem"
        >
          <span class="font-medium">O mnie</span>
          <svg class="w-5 h-5 text-amber-400 transition-transform duration-300 group-hover:translate-x-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
          </svg>
        </a>
        <a 
          href="#kontakt" 
          class="mobile-nav-link group flex items-center justify-between px-4 py-4 text-amber-800 hover:text-amber-600 hover:bg-amber-50/80 rounded-xl transition-all duration-300 focus:outline-none focus:ring-2 focus:ring-amber-300/50 focus:bg-amber-50/80"
          role="menuitem"
        >
          <span class="font-medium">Kontakt</span>
          <svg class="w-5 h-5 text-amber-400 transition-transform duration-300 group-hover:translate-x-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
          </svg>
        </a>
        
        <!-- Enhanced mobile CTA button -->
        <div class="pt-4 mt-4 border-t border-amber-200/40">
          <a 
            href="#kontakt" 
            class="mobile-nav-link group relative overflow-hidden flex items-center justify-center space-x-2 bg-gradient-to-r from-amber-600 to-orange-500 text-white px-6 py-4 rounded-xl font-semibold hover:from-amber-700 hover:to-orange-600 transition-all duration-300 shadow-lg hover:shadow-xl focus:outline-none focus:ring-4 focus:ring-amber-300/50 transform hover:scale-105 active:scale-95"
            role="menuitem"
          >
            <!-- Button shine effect -->
            <div class="absolute inset-0 bg-gradient-to-r from-transparent via-white/20 to-transparent -skew-x-12 translate-x-[-100%] group-hover:translate-x-[200%] transition-transform duration-700"></div>
            <span class="relative">Bezpłatna wycena</span>
            <svg class="relative w-5 h-5 transition-transform duration-300 group-hover:translate-x-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
            </svg>
          </a>
        </div>
      </div>
    </div>
  </div>
</header>

<style>
  /* Enhanced Hamburger Menu Styles */
  .hamburger {
    width: 24px;
    height: 18px;
    position: relative;
  }

  .hamburger-line {
    display: block;
    width: 100%;
    height: 2.5px;
    background: linear-gradient(135deg, #92400e 0%, #d97706 100%);
    border-radius: 2px;
    position: absolute;
    transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
    transform-origin: center;
  }

  .top-line {
    top: 0;
  }

  .middle-line {
    top: 7.5px;
  }

  .bottom-line {
    top: 15px;
  }

  /* Enhanced Active Hamburger Animation */
  .hamburger.active .top-line {
    transform: rotate(45deg) translateY(7.5px);
    background: linear-gradient(135deg, #d97706 0%, #ea580c 100%);
  }

  .hamburger.active .middle-line {
    opacity: 0;
    transform: scale(0);
  }

  .hamburger.active .bottom-line {
    transform: rotate(-45deg) translateY(-7.5px);
    background: linear-gradient(135deg, #d97706 0%, #ea580c 100%);
  }

  /* Enhanced Mobile Menu Animations */
  .mobile-menu-open {
    transform: translateY(0) !important;
    opacity: 1 !important;
    visibility: visible !important;
  }

  /* Staggered Animation for Mobile Menu Items */
  .mobile-nav-link {
    opacity: 0;
    transform: translateX(-20px);
    animation: slideInRight 0.3s ease-out forwards;
  }

  .mobile-menu-open .mobile-nav-link:nth-child(1) {
    animation-delay: 0.1s;
  }

  .mobile-menu-open .mobile-nav-link:nth-child(2) {
    animation-delay: 0.2s;
  }

  .mobile-menu-open .mobile-nav-link:nth-child(3) {
    animation-delay: 0.3s;
  }

  .mobile-menu-open .mobile-nav-link:nth-child(4) {
    animation-delay: 0.4s;
  }

  .mobile-menu-open .mobile-nav-link:nth-child(5) {
    animation-delay: 0.5s;
  }

  @keyframes slideInRight {
    to {
      opacity: 1;
      transform: translateX(0);
    }
  }

  /* Enhanced Scroll Behavior */
  html {
    scroll-behavior: smooth;
    scroll-padding-top: 100px; /* Accounting for sticky header */
  }

  /* Enhanced Active Link Styling */
  .nav-link.active,
  .mobile-nav-link.active {
    color: #d97706;
    background: linear-gradient(135deg, #fef3c7 0%, #fed7aa 100%);
    box-shadow: 0 4px 12px rgba(217, 119, 6, 0.15);
    border: 1px solid rgba(217, 119, 6, 0.2);
  }

  .nav-link.active::before,
  .mobile-nav-link.active::before {
    content: '';
    position: absolute;
    bottom: -2px;
    left: 50%;
    transform: translateX(-50%);
    width: 6px;
    height: 6px;
    background: #d97706;
    border-radius: 50%;
    box-shadow: 0 0 8px rgba(217, 119, 6, 0.6);
  }

  /* Focus Styles for Better Accessibility */
  .nav-link:focus-visible,
  .mobile-nav-link:focus-visible,
  button:focus-visible,
  a:focus-visible {
    outline: none;
    box-shadow: 0 0 0 3px rgba(217, 119, 6, 0.5), 0 0 0 6px rgba(245, 158, 11, 0.2);
  }

  /* Enhanced Hover Effects */
  .nav-link:hover::after {
    content: '';
    position: absolute;
    bottom: -4px;
    left: 50%;
    transform: translateX(-50%);
    width: 0;
    height: 2px;
    background: linear-gradient(90deg, #d97706, #ea580c);
    border-radius: 1px;
    animation: expandLine 0.3s ease-out forwards;
  }

  @keyframes expandLine {
    to {
      width: 80%;
    }
  }

  /* Custom Screen Reader Only Class */
  .sr-only {
    position: absolute;
    width: 1px;
    height: 1px;
    padding: 0;
    margin: -1px;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    white-space: nowrap;
    border: 0;
  }

  .focus\:not-sr-only:focus {
    position: static;
    width: auto;
    height: auto;
    padding: 0.5rem 1rem;
    margin: 0;
    overflow: visible;
    clip: auto;
    white-space: normal;
  }

  /* Enhanced Color System */
  .text-cream-50 {
    color: #fffbf0;
  }

  .bg-cream-50 {
    background-color: #fffbf0;
  }

  /* Reduced Motion Support */
  @media (prefers-reduced-motion: reduce) {
    * {
      animation-duration: 0.01ms !important;
      animation-iteration-count: 1 !important;
      transition-duration: 0.01ms !important;
    }

    html {
      scroll-behavior: auto;
    }
  }

  /* High Contrast Mode Support */
  @media (prefers-contrast: high) {
    .nav-link,
    .mobile-nav-link {
      border: 2px solid;
    }

    .hamburger-line {
      background: currentColor;
    }
  }


  /* Touch Device Optimizations */
  @media (hover: none) and (pointer: coarse) {
    .nav-link,
    .mobile-nav-link,
    button {
      min-height: 44px; /* Apple's recommended touch target size */
      min-width: 44px;
    }
  }

  /* Print Styles */
  @media print {
    header {
      position: static;
      background: white;
      box-shadow: none;
    }

    .mobile-menu-button {
      display: none;
    }
  }
</style>

<script>
  // Enhanced Mobile Menu Functionality with Improved Accessibility
  document.addEventListener('DOMContentLoaded', function() {
    const mobileMenuButton = document.getElementById('mobile-menu-button');
    const mobileMenu = document.getElementById('mobile-menu');
    
    if (!mobileMenuButton || !mobileMenu) return;
    
    const hamburger = mobileMenuButton.querySelector('.hamburger');
    const mobileNavLinks = document.querySelectorAll('.mobile-nav-link');
    let isMenuOpen = false;

    // Enhanced toggle mobile menu with keyboard support
    mobileMenuButton.addEventListener('click', handleMenuToggle);
    mobileMenuButton.addEventListener('keydown', function(event) {
      if (event.key === 'Enter' || event.key === ' ') {
        event.preventDefault();
        handleMenuToggle();
      }
    });

    function handleMenuToggle() {
      isMenuOpen = !isMenuOpen;
      
      if (isMenuOpen) {
        openMobileMenu();
      } else {
        closeMobileMenu();
      }
    }

    function openMobileMenu() {
      if (!mobileMenu || !mobileMenuButton) return;
      
      mobileMenu.classList.add('mobile-menu-open');
      if (hamburger) hamburger.classList.add('active');
      
      mobileMenuButton.setAttribute('aria-expanded', 'true');
      mobileMenuButton.setAttribute('aria-label', 'Zamknij menu nawigacyjne');
      
      // Focus management for accessibility
      const firstNavLink = mobileMenu.querySelector('.mobile-nav-link') as HTMLElement;
      if (firstNavLink) {
        setTimeout(() => firstNavLink.focus(), 100);
      }
      
      // Prevent body scroll when menu is open
      document.body.style.overflow = 'hidden';
    }

    function closeMobileMenu() {
      if (!mobileMenu || !mobileMenuButton) return;
      
      mobileMenu.classList.remove('mobile-menu-open');
      if (hamburger) hamburger.classList.remove('active');
      
      mobileMenuButton.setAttribute('aria-expanded', 'false');
      mobileMenuButton.setAttribute('aria-label', 'Otwórz menu nawigacyjne');
      
      // Return focus to menu button
      mobileMenuButton.focus();
      
      // Re-enable body scroll
      document.body.style.overflow = '';
      
      isMenuOpen = false;
    }

    // Enhanced keyboard navigation
    mobileMenu.addEventListener('keydown', function(event: KeyboardEvent) {
      if (event.key === 'Escape') {
        closeMobileMenu();
      }
      
      // Tab trapping within mobile menu
      if (event.key === 'Tab') {
        const focusableElements = mobileMenu.querySelectorAll('.mobile-nav-link') as NodeListOf<HTMLElement>;
        const firstElement = focusableElements[0];
        const lastElement = focusableElements[focusableElements.length - 1];
        
        if (event.shiftKey && document.activeElement === firstElement) {
          event.preventDefault();
          lastElement.focus();
        } else if (!event.shiftKey && document.activeElement === lastElement) {
          event.preventDefault();
          firstElement.focus();
        }
      }
    });

    // Close mobile menu when clicking on a link
    mobileNavLinks.forEach(link => {
      link.addEventListener('click', function(this: HTMLElement) {
        closeMobileMenu();
        // Smooth scroll with offset for sticky header
        const href = this.getAttribute('href');
        if (href && href.startsWith('#')) {
          const target = document.querySelector(href) as HTMLElement;
          if (target) {
            const headerHeight = document.querySelector('header')?.offsetHeight || 80;
            const targetPosition = target.offsetTop - headerHeight - 20;
            window.scrollTo({
              top: targetPosition,
              behavior: 'smooth'
            });
          }
        }
      });
    });

    // Close mobile menu when clicking outside
    document.addEventListener('click', function(event) {
      const target = event.target as HTMLElement;
      if (isMenuOpen && !mobileMenuButton.contains(target) && !mobileMenu.contains(target)) {
        closeMobileMenu();
      }
    });

    // Enhanced Active Link Highlighting with Intersection Observer
    function initializeActiveLinks() {
      const navLinks = document.querySelectorAll('.nav-link, .mobile-nav-link');
      const sections = document.querySelectorAll('section[id], main > *[id]');
      
      if (sections.length === 0) return;

      const observerOptions = {
        root: null,
        rootMargin: '-20% 0px -60% 0px',
        threshold: 0
      };

      const observer = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
          if (entry.isIntersecting) {
            const sectionId = entry.target.getAttribute('id');
            
            // Remove active class from all links
            navLinks.forEach(link => link.classList.remove('active'));
            
            // Add active class to current section links
            navLinks.forEach(link => {
              if (link.getAttribute('href') === `#${sectionId}`) {
                link.classList.add('active');
              }
            });
          }
        });
      }, observerOptions);

      sections.forEach(section => observer.observe(section));
      
      // Fallback for scroll-based detection
      function fallbackActiveDetection() {
        const scrollPosition = window.scrollY + 100;
        let currentSection = '';
        
        sections.forEach(section => {
          const htmlSection = section as HTMLElement;
          const sectionTop = htmlSection.offsetTop;
          const sectionHeight = htmlSection.offsetHeight;
          
          if (scrollPosition >= sectionTop && scrollPosition < sectionTop + sectionHeight) {
            currentSection = section.getAttribute('id') || '';
          }
        });

        navLinks.forEach(link => {
          link.classList.remove('active');
          if (link.getAttribute('href') === `#${currentSection}`) {
            link.classList.add('active');
          }
        });
      }

      // Use fallback if Intersection Observer is not supported
      if (!window.IntersectionObserver) {
        window.addEventListener('scroll', fallbackActiveDetection);
        fallbackActiveDetection();
      }
    }

    // Initialize active links
    initializeActiveLinks();

    // Enhanced header scroll effects
    let lastScrollY = 0;
    const header = document.querySelector('header');

    function handleHeaderScroll() {
      const currentScrollY = window.scrollY;
      
      if (!header) return;

      // Add/remove scroll class for styling
      if (currentScrollY > 50) {
        header.classList.add('scrolled');
      } else {
        header.classList.remove('scrolled');
      }

      lastScrollY = currentScrollY;
    }

    // Throttled scroll handler for better performance
    let scrollTimeout: number;
    window.addEventListener('scroll', function() {
      if (scrollTimeout) {
        clearTimeout(scrollTimeout);
      }
      scrollTimeout = window.setTimeout(handleHeaderScroll, 10);
    });

    // Initial scroll check
    handleHeaderScroll();

    // Prefers reduced motion support
    const prefersReducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)');
    if (prefersReducedMotion.matches) {
      // Disable smooth scrolling for users who prefer reduced motion
      document.documentElement.style.scrollBehavior = 'auto';
    }

    // Enhanced keyboard navigation for desktop nav and smooth scrolling
    const desktopNavLinks = document.querySelectorAll('nav .nav-link') as NodeListOf<HTMLElement>;
    desktopNavLinks.forEach((link, index) => {
      // Add smooth scrolling to desktop nav links
      link.addEventListener('click', function(this: HTMLElement, event: Event) {
        const href = this.getAttribute('href');
        if (href && href.startsWith('#')) {
          event.preventDefault();
          const target = document.querySelector(href) as HTMLElement;
          if (target) {
            const headerHeight = document.querySelector('header')?.offsetHeight || 80;
            const targetPosition = target.offsetTop - headerHeight - 20;
            window.scrollTo({
              top: targetPosition,
              behavior: 'smooth'
            });
          }
        }
      });

      // Keyboard navigation
      link.addEventListener('keydown', function(event: KeyboardEvent) {
        if (event.key === 'ArrowRight' || event.key === 'ArrowDown') {
          event.preventDefault();
          const nextIndex = (index + 1) % desktopNavLinks.length;
          desktopNavLinks[nextIndex].focus();
        } else if (event.key === 'ArrowLeft' || event.key === 'ArrowUp') {
          event.preventDefault();
          const prevIndex = (index - 1 + desktopNavLinks.length) % desktopNavLinks.length;
          desktopNavLinks[prevIndex].focus();
        }
      });
    });

    // Add click analytics (placeholder for future implementation)
    function trackNavigation(linkText: string, linkHref: string) {
      // Placeholder for analytics tracking
      console.log(`Navigation: ${linkText} -> ${linkHref}`);
    }

    // Track navigation clicks
    document.querySelectorAll('.nav-link, .mobile-nav-link').forEach(link => {
      link.addEventListener('click', function(this: HTMLElement) {
        const linkText = this.textContent?.trim() || '';
        const linkHref = this.getAttribute('href') || '';
        trackNavigation(linkText, linkHref);
      });
    });
  });
</script>
